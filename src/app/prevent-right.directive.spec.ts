import { PreventRightDirective } from './prevent-right.directive';

describe('PreventRightDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventRightDirective();
    expect(directive).toBeTruthy();
  });
});
